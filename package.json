{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev -o",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.2.2",
    "sass": "^1.58.3",
    "sass-loader": "^10.4.1"
  },
  "dependencies": {
    "bulma": "^0.9.4"
  }
}
